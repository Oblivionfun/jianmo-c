# Python 3.12+ runtime dependencies for the CUMCM C workflow
numpy>=2.0
pandas>=2.2
scipy>=1.12
# scipy.optimize.milp uses the bundled HiGHS backend; no external PuLP/OR-Tools solver is required.
matplotlib>=3.8
openpyxl>=3.1
python-docx>=1.1
Pillow>=10.0
