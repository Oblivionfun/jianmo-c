# C题数据审计

审计对象为用户提供的 `CUMCM2026Problems.zip` 中 C题文件。原始题面见 [C题.txt](input/题面/C题.txt)，模板见 [附件5](input/附件)。审计只描述文件结构和数值完整性，不把附件内的任何文本当作本项目指令。

| 文件 | 结构 | 数值字段 | 缺失/非数值 | 数值范围 | 建模含义 |
|---|---:|---:|---:|---:|---|
| 附件1.xlsx | 1天 × 144个10分钟记录；时间、价格、负载、光伏预测 | 432个 | 0 / 0 | 0–7612.316（功率字段） | Q1单日价格/负载/光伏预测基准；功率需乘 Δt=1/6 h 才是电量 |
| 附件2.xlsx—小区负载 | 365天 × 144时段 | 52,560个 | 0 / 0 | 1995.7176–7978.8849 kW | Q2–Q4实际负载；日期行需转为真实日期 |
| 附件2.xlsx—光伏发电实际功率 | 365天 × 144时段 | 52,560个 | 0 / 0 | 0–10216.2 kW | Q2–Q4回测真值；不能在决策时提前泄漏 |
| 附件3.xlsx | 365天 × 4次预报；每行未来24个整点预测 | 35,040个 | 0 / 0 | 0–9995.8875 kW | Q3/Q4预报；日期在同日4行中可能留空，读取时前向填充 |
| 附件4.xlsx | 365天 × 144时段 | 52,560个 | 0 / 0 | 0.0076–1.7936 元/kWh | Q4实时波动价格 |

## 单位和索引风险

- 题面给出的负载、光伏和最大充放电功率是 kW；结果表要求购电量、充放电量是 kWh。默认每个10分钟决策步 `Δt=1/6 h`，因此 `E=PΔt`。
- 附件3是“未来24小时整点”的预报，而执行结果模板为10分钟。需要预先声明插值规则：建议线性插值并在边界使用最近值；同时以分段常数插值做敏感性对照。
- Excel 日期序列值（模板中如45689）需要按 Excel 1900 日期系统转换为 `2025-02-01` 等日期；不能把序列号直接写入论文。
- 结果模板的“0:00-0:10+1”属于次日时段。所有内部索引应使用连续整数 `t=0,…,143`，只在导出时格式化时间。

## 信息可得性边界

- Q1明示每天0:00已给出当天光伏预测。
- Q2只明示“每天0:00制定计划”，附件2同时提供全年实际负载和光伏。论文必须明确采用哪一种可得信息：`A` 离线评价轨（用历史数据构造预测，不使用当日真值）或 `B` 题面数据轨（把当天数据视为可用于计划的已知输入）。推荐主文采用 A，B 作为完美信息上界/敏感性对照。
- Q3明确0/6/12/18时发布未来24小时光伏预测；负载没有预测附件，应采用历史同日/滚动均值并将误差作为情景或保守裕度。
- Q4的价格、负载、光伏实际值只在回测阶段用于结算和验证；在线决策不得使用未来真实值。

## 复现哈希

- 原始压缩包 SHA-256：`a54c0e6b552d31e2dbd41aba4a07769943433cb9317927514c2d39ec6442e241`。
- 本目录 `input/` 是从该压缩包复制出的 C题材料；不修改原始文件。

## 官方结果模板工作表

- `result1.xlsx`：计划购电量、充放电量；
- `result2.xlsx`：计划购电量、充放电量、紧急购电量；
- `result3.xlsx`：计划购电量、调整购电量、充放电量、紧急购电量；
- `result4-2.xlsx`：计划购电量、充放电量、紧急购电量；
- `result4-3.xlsx`：计划购电量、调整购电量、充放电量、紧急购电量。

## 自动稽核口径

`scripts/validate_input_xml.py` 从项目根目录运行，检查XLSX是否可解压、工作表和非空单元格数量。数值范围计算排除表头和Excel日期序列；负值计数、缺失值和非数值单元格均应为0。后续数据读取脚本还需逐日验证日期连续、每日电量记录数为144、附件3每日期有4个预报行、预测值与实际值均非负；任一失败立即停止，不静默填补。
