# 2026 CUMCM C题：题目分析报告

## 1. 研究问题与评价边界

**总目标**：在不低于小区负载的前提下，用储能跨时段搬移光伏/外网电能，最小化购电与违约、紧急补电费用；同时证明策略在预测误差、实时价格和信息更新时间变化下仍可行。

题目提供的是一个“能量平衡 + 储能状态 + 计划/调整结算”的离散优化问题。高分区分点不在于堆叠复杂算法，而在于：信息集写清楚、单位一致、没有未来信息泄漏、充放电物理可行、结算规则逐项核对，并用回测和敏感性分析证明改进。

### 题面已确认事实

- C题标题为“微网与外部电网电力调控策略”；题面、附录1和附件说明见 [input/题面/C题.txt](input/题面/C题.txt)。
- 储能容量12000 kWh，功率上限5000 kW，初始电量6000 kWh，状态约束1200–10800 kWh，效率90%。
- Q1是单日已知价格/负载/光伏预测；Q2加入全年负载和光伏实际数据并设置5倍紧急电价；Q3加入0/6/12/18时的24小时光伏预报及计划—调整结算；Q4把价格替换为附件4的实时波动序列。
- 结果模板要求输出10分钟购电量，以及4小时区间充放电和0/24时储能状态；Q2–Q4覆盖2025-02-01至12-31。

### 必须显式声明的假设

1. **时间离散**：`Δt=1/6 h`，功率乘以Δt转为电量；功率上限对应每步最多 `5000/6=833.333 kWh`。
2. **能量流方向**：`c_t`为电池端充电电量、`d_t`为电池端放电供负载电量；建议状态式 `S_{t+1}=S_t+η c_t-d_t/η`，并在附录给出另一种效率口径的敏感性。
3. **不允许同时充放电**：主模型用二进制 `z_t` 强制 `c_t≤PΔt z_t`、`d_t≤PΔt(1-z_t)`；同时报告去掉二进制后的LP结果作为速度基线，检查两者是否一致。
4. **不售电**：题面只要求购电和供电，不设上网收益；光伏剩余只能弃光或充电。若把剩余外送作为扩展，不能把它混入主结果。
5. **Q2信息轨**：主文采用“因果回测轨”：0:00只用历史数据/季节特征生成当天预测，实际数据仅用于结算；另给“完美信息轨”作为理论下界。若评委将附件2视为当天已知输入，完美信息轨可直接对应题面解释，但必须标注信息假设。
6. **Q3插值**：附件3为整点预测、模板为10分钟；主模型采用线性插值，分段常数作为敏感性对照。日期空白行前向填充。

## 2. 统一能量平衡模型

令 `L_t`为负载功率、`G_t`为实际光伏功率、`\hat G_t`为决策时可用预测，`q_t`为正常计划购电量，`e_t`为紧急购电量，`u_t`为弃光量（均为kWh）。每步的基本约束为

`q_t + e_t + G_tΔt + d_t = L_tΔt + c_t + u_t`，

`S_{t+1}=S_t+ηc_t-d_t/η`，

`1200≤S_t≤10800`，`0≤c_t,d_t≤5000Δt`，且 `c_t d_t=0`（用二进制线性化）。

当策略在决策时使用预测，优化约束中的 `G_t`、`L_t`替换为预测值；回测时用实际值重新计算紧急购电、弃光和成本，不能把实际值提前输入优化器。

## 3. 四问递进路线

### Q1：单日确定性基线（LP/MILP）

**目标**：`min Σ_t p_t q_t`。如果允许弃光，加入 `u_t≥0`；若题面不允许未利用光伏，则令 `u_t=0`并检查可行性。加入 `S_0=S_{144}`。

**两层实现**：

- LP快速基线：不加二进制，依靠效率损失和正购电成本通常会避免同时充放电；输出同时充放电检查。
- MILP主结果：加入 `z_t`，保证物理可行；将目标值、求解时间、充放电重叠量与LP对照。

**验证**：逐时段功率平衡残差、SOC上下界、端点差、购电非负、全天购电量/费用与表1–2一致。Q1结果只在完成计算后填入 `result1.xlsx`，当前阶段不虚构数值。

### Q2：全年日 ahead + 紧急购电

**基线**：每天0:00生成 `\hat L_{d,t},\hat G_{d,t}`，求解日内MILP；实际运行时按 `e_{d,t}=max(0,L_{d,t}Δt-G_{d,t}Δt-q_{d,t}-d_{d,t}+c_{d,t})` 补足缺口，并按 `5p_t`结算。

**预测候选（从易到难）**：

1. 持续性/同日历史中位数（必须有）；
2. 按月份、工作日/周末、时段的分层均值或分位数；
3. 误差校准：用历史 `G-\hat G` 建立时段和预测跨度的分位数区间；
4. 只有在回测证明有增益时，才加入梯度提升/量化回归等模型。

**增强模型**：以紧急电价5倍为权重做保守裕度：`G_t^{safe}=\hat G_t-q_{α,t}`，或建立少量历史误差场景 `ω`，目标为期望成本 + `λ CVaR_α`。主文最多保留一个增强系统，避免模型堆砌。

**全年评价**：总成本、紧急购电量/天、紧急购电费用占比、弃光率、SOC越界次数、最大购电功率、计算时间；与无储能、贪心低价充电、完美信息下界比较。

### Q3：多时刻预报 + 计划/调整结算

令 `q_t^0`为0:00计划，`q_t^a`为后续预报时刻产生的调整量。结算差额可写为

`C_t=p_t q_t^0+0.5p_t(q_t^0-q_t^a)^+ +1.5p_t(q_t^a-q_t^0)^+ +5p_t e_t`。

在0/6/12/18时执行滚动优化：固定已经发生的功率与SOC，只优化未来窗口；计划与调整的差额由上式结算。建议比较：

- 只用0:00预报；
- 0:00计划 + 6/12/18全部调整；
- 仅保留带来净收益的更新时间；
- “预测变化超过阈值才重优化”的自适应策略。

“是否需要其他时刻预报”用增量价值回答：对每个更新时间 `τ`，计算相对无该更新时间的成本下降、紧急购电下降和新增调整罚金；用日期分层的置信区间报告结果，而不是只给一个平均数。若6时预报改善最大、12/18时边际收益小，可在论文中说明这源于剩余可调时长与调整价格的权衡。

### Q4：波动电价

把 `p_t`替换为附件4的实时价格，同时保留Q2或Q3的信息集和实际回测规则。由于价格波动会改变储能机会成本，主模型可加入“价格峰谷识别 + SOC安全储备”解释变量，但不需要另造黑箱算法。

- Q4-2：Q2框架，目标含正常购电和5倍紧急购电；分析价格波动对充电时机、峰值购电和紧急费用的影响。
- Q4-3：Q3框架，计划/调整差额仍按交易时刻 `p_t`的0.5/1.5倍结算；比较固定电价下的调整价值是否在波动价格下增大。

## 4. 可形成高分辨识度的主线

**推荐主线**：`确定性MILP → 因果预测误差校准 → 计划/调整滚动MPC → 少量场景或CVaR风险项 → 严格全年回测`。

辨识度来自四个可检验设计：

1. **信息集审计**：明确0:00能看到什么，避免“用当天实际值预测当天”的未来泄漏；
2. **不对称损失**：5倍紧急电价和Q3的0.5/1.5倍差额直接进入目标，而非事后解释；
3. **预测时刻的边际价值**：将每个新增预报时刻视为可比较的决策资源；
4. **鲁棒性而非单点最优**：按季节、天气型、工作日分层回测，报告成本—安全—弃光的权衡。

## 5. 失败模式与反例检查

- **同时充放电**：若LP出现同一时段 `c_t,d_t>0`，必须切换MILP或修正目标；不能直接提交。
- **功率/电量混用**：结果表填kWh；任何kW值必须乘1/6。
- **SOC效率方向错误**：分别做充电端/电池端口径对照，检查全天能量守恒。
- **跨日状态错误**：Q1要求日初日末相同；Q2–Q4应把前一日24:00状态传给下一日0:00（若题面解释要求每日独立，则另做独立重置敏感性）。
- **泄漏**：训练/预测必须按日期向前；附件2实际光伏只用于结算和回放。
- **只报均值**：获奖论文需要至少一个基线、一个消融、一个分层误差分析和一个不可行/极端日检查。

## 6. 当前可执行任务顺序

1. 建立数据读取、日期前向填充、单位换算和模板写入测试；
2. 先在附件1上跑Q1 LP/MILP并生成功率平衡/SOC图；
3. 选定Q2预测器，以滚动时间切分做全年回测；
4. 将Q2求解器改造成Q3滚动调整，测4个发布时间的增量价值；
5. 替换附件4价格完成Q4；
6. 用独立复核清单审计公式、信息集、模板和结果文件，再写论文。

## 附：模型合同总表

| 子题 | 任务定义 | 数据 | 结论类型 | 模型 | 数学核心 | 实现 | 验证与风险 |
|---|---|---|---|---|---|---|---|
| Q1 | 单日制定购电与储能计划 | 附件1、储能参数 | 购电量、费用、SOC与充放电表 | 确定性LP/MILP（一个模型族） | 能量平衡、SOC递推、端点相等 | SciPy LP + MILP求解器 | 平衡残差、SOC边界、LP/MILP重叠检查 |
| Q2 | 每日0:00计划，实际不足时紧急补电 | 附件1价格、附件2全年负载/PV | 年度成本、紧急电量、指定日期表 | 因果预测 + 日 ahead MILP；误差分位数为同族增强 | 预测误差裕度/少量场景、5倍紧急价 | 日期滚动回测、统一求解器 | 无储能/贪心/完美信息基线；防泄漏 |
| Q3 | 0/6/12/18预报驱动计划和调整 | 附件2、附件3、附件1价格 | 调整费用、紧急量、更新时间价值 | 滚动MPC（计划—调整结算） | 0.5/1.5倍差额正部函数 | 固定已执行段、滚动重优化 | 留出日期、更新时间消融、增量价值 |
| Q4 | 波动价格下重算Q2/Q3 | 附件2–4 | 波动价格成本和策略变化 | 同Q2/Q3模型族替换价格输入 | 价格时序进入目标与结算 | 统一回测与输出模板 | 价格峰谷敏感性、极端日检查 |

### 统一离散量约定（M1修正）

以下变量均表示一个10分钟决策步内的电量（kWh）：`c_t,d_t,q_t,e_t,u_t`；若输入是功率（kW），统一先乘 `Δt=1/6 h`。附件2–4从2025-01-01开始，结果输出从2025-02-01开始；内部先按日期解析并验证365天连续性，再截取日期 `2025-02-01` 至 `2025-12-31`，不靠行号硬切。
